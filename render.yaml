services:
  - type: web
    name: taskflow-backend
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    # UPDATED: A more direct start command
    startCommand: "cd backend && gunicorn app:app"
