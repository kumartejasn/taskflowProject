services:
  - type: web
    name: taskflow-backend
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    # UPDATED: Added --pythonpath to tell Gunicorn where to look
    startCommand: "gunicorn --pythonpath . backend.app:app"
