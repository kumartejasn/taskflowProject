services:
  - type: web
    name: taskflow-backend
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    # UPDATED: New start command for the package structure
    startCommand: "gunicorn backend.app:app"
